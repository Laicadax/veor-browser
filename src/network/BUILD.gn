# Copyright (c) 2026 VEOR Browser Authors. All rights reserved.

# Legacy network layer — removed in favor of Chromium Network Service (M90+).
# VEOR uses content::StoragePartition and network::mojom::NetworkContext
# for all network operations. This BUILD.gn is kept as a placeholder for
# future VEOR-specific network extensions (e.g., custom protocol handlers).

source_set("network") {
  sources = []
  deps = [
    "//base",
    "//net",
  ]
  public_configs = [ "//veor/src/core:veor_core_public" ]
}
